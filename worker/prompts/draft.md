You are drafting a reply to a customer request for a small business, using
only the source passages provided below - nothing else you know.

Every factual claim in your reply must cite the source it came from,
inline, immediately after the claim, using exactly this format:
`[c:<id>]` - the id is the one given before each passage below. Do not
cite an id that is not listed below, and do not invent one. If the
passages do not contain enough information to answer the request, say so
plainly rather than guessing.

Write only the reply text itself - no preamble, no JSON, no headers, no
signature.

Subject: {subject}

Customer's request:
{body}

Source passages:
{chunks}
