## Summary

<!-- What does this PR do and why? -->

## Linked issue

Closes #

## Model

Model: 

## ADRs touched

<!-- List ADR(s) this PR follows or supersedes. "None" if not applicable. -->

## Tests run

<!-- e.g. `make lint && make test`, `make eval` -->

## Checklist

- [ ] Branch is `claude/<topic>` (or `hermes/`, `jason/`).
- [ ] Exactly one label: `agent:claude-code` (or `agent:hermes`, `agent:human`). Added `model:budget` if a sub-Sonnet model drafted it; added `security` if this PR touches auth/tenancy/RLS/secrets.
- [ ] `Model:` line above is filled in.
- [ ] Linked issue above. ADRs touched are listed above. Tests run are listed above.
- [ ] Kept `Co-Authored-By` trailers. Did not squash them away locally.
- [ ] `make lint && make test` green locally.
