{{- if .Values.web.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "frontdesk.web.fullname" . }}
  labels:
    {{- include "frontdesk.web.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "frontdesk.web.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.web.networkPolicy.fromNamespace }}
      ports:
        - protocol: TCP
          port: {{ .Values.web.service.targetPort }}
{{- end }}
