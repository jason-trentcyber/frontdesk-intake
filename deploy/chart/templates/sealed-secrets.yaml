{{/*
Applies every SealedSecret file under sealed/ verbatim, as part of this
release. Files are sealed by a human for namespace frontdesk (README.md ->
Secrets) - nothing here creates, reads, or renders a plaintext secret. The
range is empty (and this template a no-op) until sealed/turnstile.sealed.yaml
is added.
*/}}
{{- range $path, $_ := .Files.Glob "sealed/*.yaml" }}
---
{{ $.Files.Get $path }}
{{- end }}
