# Pre-existing content docs, out of scope for #8 (toolchain scaffolding).
# Reformatting them is a separate, deliberate change, not a side effect of adding prettier.
*.md
pnpm-lock.yaml

# Generated by z.toJSONSchema() (api/src/queue/contract.test.ts asserts the
# committed file equals a fresh regeneration byte-for-byte, #23) - not
# hand-written. Prettier would collapse the "required" array onto one line,
# which JSON.stringify(schema, null, 2) never does; formatting it here would
# just make it fail the drift check on the next regeneration. Same reasoning
# as pnpm-lock.yaml above: an exact-match generated file, not prose to style.
docs/contracts/triage-message.schema.json
